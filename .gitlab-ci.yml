stages:
  - build
  - test

variables:
  GIT_SUBMODULE_STRATEGY: recursive

beekeeper_native_build:
  stage: build
  image: registry.gitlab.syncad.com/hive/common-ci-configuration/ci-base-image:ubuntu24.04-py3.14-2@sha256:1ca0a536d22f738dd0eee514fd84f4479243a6d92e44981d00da9f67240b5083
  script:
    - mkdir -p build && cd build
    - cmake -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTS=ON ..
    - make -j$(nproc)
  artifacts:
    paths:
      - build/programs/beekeeper/beekeeper/beekeeper
      - build/tests/unit/beekeeper_test
    expire_in: 1 week
  tags:
    - public-runner-docker

beekeeper_native_test:
  stage: test
  image: registry.gitlab.syncad.com/hive/common-ci-configuration/ci-base-image:ubuntu24.04-py3.14-2@sha256:1ca0a536d22f738dd0eee514fd84f4479243a6d92e44981d00da9f67240b5083
  needs:
    - job: beekeeper_native_build
      artifacts: true
  script:
    - ./build/tests/unit/beekeeper_test --log_level=test_suite
  tags:
    - public-runner-docker

beekeeper_wasm_build:
  stage: build
  image: registry.gitlab.syncad.com/hive/common-ci-configuration/emsdk:4.0.22-1
  variables:
    SOURCE_DIR: "${CI_PROJECT_DIR}/programs/beekeeper/beekeeper_wasm"
  before_script:
    - git config --global --add safe.directory '*'
    - . "${NVM_DIR}/nvm.sh"
    - nvm use "${NODEJS_VERSION}"
  script:
    # Build WASM
    - ./scripts/build_wasm_beekeeper.sh 1 "${CI_PROJECT_DIR}"
    # Install npm dependencies and build TypeScript
    - cd "${SOURCE_DIR}"
    - pnpm install --frozen-lockfile
    - pnpm build
  artifacts:
    paths:
      - "${SOURCE_DIR}/dist"
      - "${SOURCE_DIR}/src/build/*.wasm"
      - "${SOURCE_DIR}/src/build/*.js"
      - "${SOURCE_DIR}/src/build/*.d.ts"
      - "${SOURCE_DIR}/src/build/node"
      - "${SOURCE_DIR}/src/build/web"
    expire_in: 1 week
  tags:
    - public-runner-docker

beekeeper_wasm_test:
  stage: test
  image: registry.gitlab.syncad.com/hive/common-ci-configuration/emsdk:4.0.22-1
  variables:
    SOURCE_DIR: "${CI_PROJECT_DIR}/programs/beekeeper/beekeeper_wasm"
  needs:
    - job: beekeeper_wasm_build
      artifacts: true
  before_script:
    - git config --global --add safe.directory '*'
    - . "${NVM_DIR}/nvm.sh"
    - nvm use "${NODEJS_VERSION}"
  script:
    - cd "${SOURCE_DIR}"
    - pnpm install --frozen-lockfile
    # Install playwright browser
    - pnpm exec playwright install chromium
    # Run tests
    - pnpm test
  artifacts:
    paths:
      - "${SOURCE_DIR}/results.xml"
      - "${SOURCE_DIR}/results.json"
    reports:
      junit: "${SOURCE_DIR}/results.xml"
    when: always
    expire_in: 1 week
  tags:
    - public-runner-docker
